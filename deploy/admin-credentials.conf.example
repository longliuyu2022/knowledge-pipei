# 安装到 /etc/systemd/system/soulmatch.service.d/admin.conf 后 daemon-reload 并重启。
# 密码哈希通过 scripts/set-admin-password.mjs 生成，保存在仓库外的 0600 文件。
[Service]
Environment=SOUL_ADMIN_USERNAME=admin
LoadCredential=admin_password_hash:/etc/soulmatch/admin_password_hash
