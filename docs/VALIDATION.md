# 同知 2.0.0 验证记录

验证日期：2026-09-13。机器可读汇总见 [validation-tongzhi.json](../artifacts/validation-tongzhi.json)。

| 范围 | 结果 |
| --- | --- |
| 后端与迁移测试 | `npm test`，191/191，通过 |
| 生产构建 | `npm run build`，TypeScript 与 Vite 通过 |
| 问题小组浏览器 | 19/19，通过 |
| 邮箱、画像、匹配与消息浏览器 | 13/13，通过 |
| 双人 AI 授权及自动话题浏览器 | 5/5，通过 |
| 管理后台治理浏览器 | 10/10，通过 |
| 管理后台既有安全回归 | 21/21，通过 |
| 公网 HTTPS 与部署 | 健康、静态资源、安全头、私有文件拒绝、用户/后台隔离、旧管理员凭证和退出通过 |
| 公网页面 | 1440 / 390 / 320 px，无页面错误、资源错误或横向溢出 |
| 模型实连 | 指定 metapi 站点、`deepseek-v4-flash`，隔离样例约 3.4 秒，返回模型结果及有效证据引用 |
| 正式数据迁移 | 22 账号、4 问题小组；79 条来源映射、99 处引用通过，未导入会话和 OAuth 令牌 |

后端覆盖持久匹配与重启恢复、双确认事务、幂等消息、权限/画像/授权竞态、个人发言建议、源撤回后的读取与导出、邮箱密码兼容、审核申诉、管理员会话与 CSRF、迁移幂等及故障回滚。模型不可用、坏 JSON 与无效引用均有明确回退或拒绝行为。

五套浏览器报告位于 `artifacts/browser-*.json`，截图中的账号、消息与资料均为隔离测试样例。公网验证仅建立并清理自己的临时访客；不向真实匹配池添加测试画像，不替开发者完成 OAuth 授权，也不为模型测试发送真人材料。

## 知乎授权事实

- 原同频项目的真实五项用户接口已由用户在 **2026-09-13 13:52** 验收：创作、关注、收藏夹读取成功，收藏夹内容及近期收藏为空数据。该历史事实不作为新域名的真人登录结果。
- 用户已确认登记新回调 **https://zhihu.aiimage.icu/auth/callback**。
- 新站授权 URL 使用 App `400`、准确回调与独立 state；缺失或错误 state 拒绝，不进行令牌交换。自动化 OAuth 回归通过。
- **新域名的真实最终授权：等待开发者本人确认。** 已提示用户打开新站亲自完成。依据 [知乎 Skill OAuth 指引](https://zhstatic.zhihu.com/skill/zhihu-hackathon-skill_v2026s2.zip)，自动化结果不能代替人的授权确认。

## 迁移范围与限制

源快照时间为 **2026-09-13 17:25:31（Asia/Shanghai）**。已运行 `apply` 与启动前 `validate`，原两站继续独立运行；后续写入不自动同步。旧访客没有已验证身份时不能仅凭昵称认领，旧管理员的普通邮箱角色也不自动升级为新站独立后台权限。

旧讨论因无法可靠重建分轮，保留在明确标记的历史轮。无法恢复的成果旧版不伪造，未能核验新 AI 授权边界的旧派生内容保守隐藏。私有迁移报告和备份留在服务器受控目录，仓库只保留聚合验证信息。
